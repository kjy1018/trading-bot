services:
  - type: worker
    name: mirae-bot-worker
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python -u scheduler.py
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
